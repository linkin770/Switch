# 办公文件跨格式本地化批量转换工具 - 打包指南

## 目标

将项目封装成完全离线应用，支持：
- Windows 平台：生成 .exe 可执行文件，包含所有依赖
- Linux 平台：生成 .deb 安装包，包含所有依赖

## 依赖管理

### 完全离线依赖准备

为了创建完全离线的安装包，需要提前封装以下依赖：

1. **Python 解释器**：
   - Windows：需要将 Python 解释器打包到安装包中
   - Linux：通常系统已预装 Python，但建议确保依赖版本兼容

2. **Python 依赖包**：
   - 所有在 `requirements.txt` 中列出的包
   ```bash
   # 创建依赖目录
   mkdir -p offline_deps
   
   # 下载所有依赖
   pip download -r requirements.txt -d offline_deps
   ```

3. **外部依赖**：
   - **LibreOffice**：必需，用于处理 Office 文件转换
     - Windows：下载 LibreOffice 离线安装包
     - Linux：下载 LibreOffice 离线安装包

4. **项目文件**：
   - 主应用文件：`app.py`
   - 前端文件：`index.html`、`static/script.js`、`static/icon.png`
   - 工具模块：`tools/` 目录下的所有文件
   - 配置文件：`config.py`

## Windows 平台打包

### 工具选择

使用 **PyInstaller** 生成 .exe 可执行文件，包含所有依赖

### 步骤

1. **安装 PyInstaller**：
   ```bash
   pip install pyinstaller
   ```

2. **创建打包配置文件** (`pyinstaller.spec`)：
   ```python
   # -*- mode: python ; coding: utf-8 -*-
   
   block_cipher = None
   
   a = Analysis(['app.py'],
                pathex=['.'],
                binaries=[],
                datas=[
                    ('index.html', '.'),
                    ('static/', 'static/'),
                    ('tools/', 'tools/'),
                    ('config.py', '.')
                ],
                hiddenimports=[
                    'weasyprint',
                    'pymupdf',
                    'pdf2docx',
                    'python_docx',
                    'pandas',
                    'markdown'
                ],
                hookspath=[],
                runtime_hooks=[],
                excludes=[],
                win_no_prefer_redirects=False,
                win_private_assemblies=False,
                cipher=block_cipher,
                noarchive=False)
   pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)
   exe = EXE(pyz,
             a.scripts,
             a.binaries,
             a.zipfiles,
             a.datas,
             [],
             name='文件转换工具',
             debug=False,
             bootloader_ignore_signals=False,
             strip=False,
             upx=True,
             upx_exclude=[],
             runtime_tmpdir=None,
             console=False,
             disable_windowed_traceback=False,
             target_arch=None,
             codesign_identity=None,
             entitlements_file=None)
   ```

3. **执行打包命令**：
   ```bash
   pyinstaller pyinstaller.spec
   ```

4. **打包结果**：
   - 可执行文件位于 `dist/文件转换工具.exe`
   - 包含所有 Python 依赖的完整目录

5. **创建完全离线安装程序**：
   - 使用 NSIS 或 Inno Setup 创建安装程序
   - 包含以下内容：
     - PyInstaller 生成的可执行文件
     - LibreOffice 离线安装包
     - 安装脚本，自动安装 LibreOffice 和设置环境
   - 安装流程：
     1. 安装 LibreOffice
     2. 复制应用文件到目标目录
     3. 创建桌面快捷方式
     4. 配置必要的环境变量

## Linux 平台打包

### 工具选择

使用 **stdeb** 生成 .deb 安装包，包含所有依赖

### 步骤

1. **安装打包工具**：
   ```bash
   sudo apt install python3-stdeb
   ```

2. **创建 setup.py 文件**：
   ```python
   from setuptools import setup, find_packages
   
   setup(
       name='file-converter',
       version='1.0.0',
       packages=find_packages(),
       include_package_data=True,
       data_files=[
           ('share/file-converter', ['index.html']),
           ('share/file-converter/static', ['static/script.js', 'static/icon.png']),
           ('share/file-converter/tools', ['tools/__init__.py', 'tools/office_tools.py', 'tools/pdf_tools.py', 'tools/zip_tools.py']),
           ('share/file-converter', ['config.py'])
       ],
       entry_points={
           'console_scripts': [
               'file-converter=app:app.run'
           ]
       },
       install_requires=[
           'Flask==1.1.4',
           'pdf2docx==0.5.8',
           'PyMuPDF==1.24.11',
           'python-docx==1.1.2',
           'pandas==2.0.3',
           'markdown==3.7',
           'weasyprint==61.2',
           'fpdf2==2.8.3'
       ],
       classifiers=[
           'Programming Language :: Python :: 3',
           'License :: OSI Approved :: MIT License',
           'Operating System :: POSIX :: Linux',
       ],
   )
   ```

3. **准备离线依赖**：
   - 将下载的 Python 依赖包复制到项目目录
   - 创建 `debian/` 目录并配置依赖

4. **生成 .deb 包**：
   ```bash
   python3 setup.py --command-packages=stdeb.command bdist_deb
   ```

5. **打包结果**：
   - .deb 包位于 `deb_dist/` 目录

6. **创建完全离线安装包**：
   - 包含以下内容：
     - 生成的 .deb 包
     - LibreOffice 离线安装包
     - 安装脚本，自动安装所有依赖
   - 安装流程：
     1. 安装 LibreOffice
     2. 安装文件转换工具 .deb 包
     3. 安装所有必要的依赖

7. **处理依赖**：
   - 在 deb 包中添加 LibreOffice 作为依赖
   ```bash
   # 在 debian/control 文件中添加
   Depends: python3, libreoffice
   ```
   - 对于其他 Python 依赖，可以使用 `dpkg-deb` 命令将依赖包添加到 deb 包中

## 完全离线部署说明

### Windows 部署

1. **准备离线安装包**：
   - 完整的安装程序（包含所有依赖）
   - 无需额外下载任何文件

2. **安装步骤**：
   1. 运行安装程序
   2. 按照提示完成安装（自动安装 LibreOffice 和文件转换工具）
   3. 安装完成后，桌面会出现快捷方式
   4. 双击快捷方式启动应用

### Linux 部署

1. **准备离线安装包**：
   - 完整的安装包（包含所有依赖）
   - 无需额外下载任何文件

2. **安装步骤**：
   ```bash
   # 解压安装包
   tar -xzf file-converter-offline.tar.gz
   
   # 进入安装目录
   cd file-converter-offline
   
   # 运行安装脚本
   sudo ./install.sh
   ```

   安装脚本会自动：
   1. 安装 LibreOffice
   2. 安装文件转换工具
   3. 安装所有必要的依赖
   4. 创建启动脚本

3. **启动应用**：
   ```bash
   # 启动文件转换工具
   file-converter
   ```

## 注意事项

### 完全离线打包注意事项

1. **依赖版本**：
   - 确保所有依赖版本兼容
   - 避免版本冲突，特别是 Python 依赖

2. **文件大小**：
   - 完全离线安装包会比较大（可能超过 1GB），因为包含了 LibreOffice 和所有 Python 依赖
   - 考虑使用压缩工具减小安装包大小

3. **平台兼容性**：
   - 确保打包的应用与目标平台的操作系统版本兼容
   - 对于 Windows，需要考虑 32 位和 64 位系统
   - 对于 Linux，需要考虑不同发行版的差异

4. **权限问题**：
   - Windows：确保安装程序有管理员权限
   - Linux：确保安装脚本有执行权限，使用 sudo 运行

5. **路径处理**：
   - 确保所有文件路径处理都是平台无关的
   - 使用绝对路径或相对路径时考虑平台差异

6. **测试**：
   - 在目标平台上进行充分测试，确保所有功能正常
   - 在干净的系统上测试安装，确保无需联网即可完成安装

7. **LibreOffice 依赖**：
   - 确保打包的 LibreOffice 版本与应用兼容
   - 对于 Windows，确保 LibreOffice 路径被正确检测

## 故障排除

### 常见问题

1. **LibreOffice 未找到**：
   - 检查安装包中是否包含 LibreOffice
   - 检查 LibreOffice 安装是否成功
   - 检查 LibreOffice 路径是否在系统环境变量中

2. **依赖缺失**：
   - 确保所有依赖都已正确打包
   - 检查安装日志，查看是否有依赖安装失败
   - 对于 Linux，使用 `dpkg -i` 手动安装缺失的依赖包

3. **权限错误**：
   - 确保应用有读写权限
   - 在 Linux 上，使用 sudo 运行应用
   - 在 Windows 上，以管理员身份运行安装程序

4. **路径错误**：
   - 检查所有文件路径处理是否正确
   - 确保使用绝对路径或相对路径时考虑平台差异
   - 检查临时文件目录是否有写权限

5. **启动失败**：
   - 检查应用日志，查看具体错误信息
   - 检查 Python 解释器是否正确打包
   - 检查依赖是否正确安装

6. **转换失败**：
   - 检查 LibreOffice 是否正确安装
   - 检查输入文件是否损坏
   - 检查临时文件目录是否有足够空间

## 总结

通过以上步骤，您可以创建完全离线的安装包，用户在新机器上直接安装就能使用，不需要联网下载任何依赖。在打包过程中，确保正确处理所有依赖和路径问题，以确保应用在离线环境中正常运行。

### 最终交付物

- **Windows 平台**：一个包含所有依赖的安装程序（.exe 或 .msi）
- **Linux 平台**：一个包含所有依赖的安装包（.tar.gz 或 .deb）
- **文档**：安装说明和使用指南

这样，用户就可以在完全离线的环境中安装和使用文件转换工具，无需联网下载任何依赖。